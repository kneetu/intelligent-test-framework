apiVersion: apps/v1
kind: Deployment
metadata:
  name: intelligent-test-generator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: intelligent-test-generator
  template:
    metadata:
      labels:
        app: intelligent-test-generator
    spec:
      containers:
        - name: generator
          image: intelligent-test-generator:latest
          env:
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: api-keys
                  key: openai-api-key
